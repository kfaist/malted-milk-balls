# Commands to push to GitHub (run these after creating the repo)

# Rename branch to main (if needed)
git branch -M main

# Add GitHub remote
git remote add origin https://github.com/kfaist/malted-milk-balls.git

# Push to GitHub
git push -u origin main
